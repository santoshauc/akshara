{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "SigningKey": "dev-only-signing-key-not-for-production-use-0123456789abcdef",
    "_comment_RequirePlatformMfa": "Dev only. Production leaves this at its true default, so an operator must enrol an authenticator before any platform screen serves them.",
    "RequirePlatformMfa": false
  }
}
