@inherits LayoutComponentBase

@* Bare layout for unauthenticated pages (login) — no app chrome, but the
   same design system so sign-in does not look like a different product. *@
<MudThemeProvider Theme="_theme" />
<MudDialogProvider />
<MudSnackbarProvider />

@Body

@code {
    private readonly MudTheme _theme = AksharaTheme.Build(null, null);
}
